// ファイル名に複数の単語を使用している場合は、アンダースコアを使用して単語を区切ります。
// たとえば、helloworld.rsではなくhello_world.rsを使用します。
fn main() {
    // main機能は特殊で実行可能なすべてのRustプログラムで
    // 最初に実行される機能
    // Rustスタイルはタブではなく4つのスペースでインデント
    println!("Hello, world!");
}
